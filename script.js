// Esperamos a que el DOM se cargue completamente
document.addEventListener("DOMContentLoaded", function() {
    // Accedemos al elemento con id "soylend" y cambiamos su contenido
    document.getElementById("soylend").textContent = "!No me gusta la programación¡";
});

